# /implement

If $ARGUMENTS is empty, respond with exactly the following block and stop:

---

## implement — Implement a Feature from a Requirements Document

To get started, provide the path to a requirements `.md` file:

**Requirements file** *(required)*
Select a `.md` file from the `requirements/` folder. This is the document generated by `/plan-feature` that describes the feature to build.

---

**Usage:** `/implement @requirements/<file>.md`

**Example:** `/implement @requirements/REQ-FR-001-FE-user-login.md`

> Tip: Type `@requirements/` to browse available requirement files.

---

## Step 0 — Hard Stop: Require REQ Document

**Before doing anything else**, check whether the user has provided a REQ `.md` file as an argument or attached it to this command.

If NO `.md` file is provided, **stop immediately** and respond with:

> **Error: No REQ document provided.**
>
> The `/implement` command requires a REQ specification file.
>
> Usage: `/implement <path-to-req-file.md>`
>
> Example: `/implement requirements/REQ-FR-001-FE-user-login.md`
>
> Please provide the REQ `.md` file and run the command again.

**Do not proceed past this point without a valid `.md` file.**

---

## Step 1 — Load Project Rules

Read all four rule files before writing any code:

- @.claude/PROJECT_DETAILS.md
- @.claude/PROJECT_STRUCTURE.md
- @.claude/CODING_STANDARDS.md
- @.claude/LIBRARY_ALLOWLIST.md

---

## Step 2 — Parse the REQ Document

Read the provided REQ `.md` file in full. Extract:

- Feature name and scope
- Steps / screens involved
- Fields, validation rules, and error messages
- API endpoints and payload shapes
- Conditional logic (membership type, firm type, etc.)
- File upload specs (types, sizes, limits) — note which steps need a `*.service.ts`
- Date fields — note where `moment` will be used
- State transitions and lock rules

---

## Step 3 — Clarify Before Coding

If any requirement is ambiguous or conflicts with a project rule, **ask the user to clarify before writing any code**. List all questions at once — do not ask one at a time.

Flag explicitly if a requirement would force a rule violation (e.g., a library not in @.claude/LIBRARY_ALLOWLIST.md).

---

## Step 4 — Output an Implementation Plan

Before writing code, output a plan that lists:

1. Files to create (with full paths per @.claude/PROJECT_STRUCTURE.md)
2. Files to modify (with reason)
3. Approach for forms, state, API hooks, and routing
4. Any conditional logic that needs special handling

Wait for the user to approve the plan before proceeding. For small, isolated changes (single component, no routing changes), you may proceed without explicit approval — use judgment.

---

## Step 5 — Implement

Write code strictly following:

- **@.claude/PROJECT_STRUCTURE.md** — exact folder and file placement
- **@.claude/CODING_STANDARDS.md** — naming, patterns, no business logic in pages
- **@.claude/LIBRARY_ALLOWLIST.md** — only approved libraries
- **@.claude/PROJECT_DETAILS.md** — domain rules, membership type logic, field specs

### Mandatory implementation checklist

- [ ] All forms use **Formik** + **Yup** schemas in `*.schema.ts`
- [ ] Every field has: label · input · Yup validation · error message from REQ
- [ ] Multi-step forms: validate **current step's schema only** on Next; full schema on final submit
- [ ] API hooks live in `*.api.ts` — never called directly from components
- [ ] File upload / download logic lives in `*.service.ts` under `src/services/`
- [ ] All pages are **lazy-loaded** via `React.lazy`
- [ ] No `any` types — all API responses fully typed
- [ ] No `dangerouslySetInnerHTML`
- [ ] No raw `style` props — Tailwind classes only; use design tokens (`primary-*`, `text-heading-*`, etc.) from `tailwind.config.js`
- [ ] No function exceeds 100 lines; one responsibility per file
- [ ] All file names are **kebab-case** (e.g. `application-form.tsx`, `applicant.api.ts`)

---

## Step 6 — Self-Review

Before reporting done, verify against all four rule files:

| Check | Rule source |
|-------|-------------|
| File extensions: `.tsx` only if JSX present | @.claude/PROJECT_STRUCTURE.md §1 |
| File names are kebab-case | @.claude/CODING_STANDARDS.md §Naming |
| Files placed in correct folders | @.claude/PROJECT_STRUCTURE.md §2 |
| No unlisted library imported | @.claude/LIBRARY_ALLOWLIST.md |
| Naming conventions followed | @.claude/CODING_STANDARDS.md |
| No business logic in page components | @.claude/CODING_STANDARDS.md |
| Multi-step: current step schema validated on Next, full schema on submit | @.claude/CODING_STANDARDS.md §Forms |
| File upload/download logic in `*.service.ts`, not in components or `*.api.ts` | @.claude/PROJECT_STRUCTURE.md §2 |
| design tokens used (`primary-*`, `text-heading-*`) — no arbitrary colors | @.claude/CODING_STANDARDS.md §UI |
| Date handling uses `moment` — no custom date parsing | @.claude/LIBRARY_ALLOWLIST.md |
| No function exceeds 100 lines | @.claude/CODING_STANDARDS.md §UI |
| Auth routes inaccessible to logged-in users | @.claude/CODING_STANDARDS.md §Routing |
| Validation error messages match REQ exactly | @.claude/PROJECT_DETAILS.md |
| Conditional fields match membership/firm type rules | @.claude/PROJECT_DETAILS.md §5 |

Report any deviations found and how they were resolved. If a deviation could not be resolved without violating a rule, flag it to the user.

---

## Step 7 — Output Completion Message (MANDATORY — DO NOT SKIP)

> **CRITICAL:** Skipping this step is a failure. Write/Edit tool calls alone are NOT sufficient output. The user cannot see tool calls — they only see assistant message text. If you do not send this message, the user will see a blank response and think the command failed.

After all Write/Edit tool calls are complete, immediately output the following block as plain assistant message text **in the same response turn**. Fill in the bracketed values with real data:

---

**Feature implemented successfully.**

- **REQ:** `{REQ filename used}`
- **Files created:** {count}
  - `{file path 1}`
  - `{file path 2}`
  - *(continue for each file)*
- **Files modified:** {count}
  - `{file path 1}`
  - *(continue for each file)*
- **Summary:** {one-line description of what was implemented}

---

Do not end your response without this block appearing as visible text to the user.
